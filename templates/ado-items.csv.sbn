Work Item Type,Title,Description,Acceptance Criteria,Severity,Tags,Source URL,Rule Reference,Evidence
{{ for finding in scan.findings }}User Story,"'Fix accessibility issue: {{ finding.issue_type }}","'{{ finding.evidence }}","The affected element meets the referenced accessibility rule. Keyboard navigation still works. The same crawler check no longer reports the issue. Manual accessibility validation is completed where required.",{{ finding.severity }},"'accessibility;{{ finding.standard }};{{ finding.rule_id }}","'{{ finding.page_url }}","'{{ finding.standard }} {{ finding.success_criterion }} {{ finding.rule_id }}","'{{ finding.evidence }}"
{{ end }}
